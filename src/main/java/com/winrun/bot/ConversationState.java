package com.winrun.bot;

public enum ConversationState {
    IDLE,
    SELECT_PRODUCT,
    SELECT_COLOR,
    SELECT_SIZE,
    ENTER_NAME,
    ENTER_PHONE,
    CHOOSE_DELIVERY,
    ENTER_DELIVERY_FIELDS,
    REVIEW,
    PAYMENT_LINK_ISSUED
}